class Solution {
    public int maximumWealth(int[][] accounts) {
        int a = 0;
        for(int[] i : accounts){
            int s = 0;
            for(int j : i){
                s += j; 
            }
            a = Math.max(a, s);
        }

        return a;
    }
}
