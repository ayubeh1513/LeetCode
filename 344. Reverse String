class Solution {
    public void reverseString(char[] s) {
        int a=0;
        int e=s.length - 1;
        while(a<e){
            char t = s[a];
            s[a]=s[e];
            s[e]=t;
            a++;
            e--;
        }
    }
}
