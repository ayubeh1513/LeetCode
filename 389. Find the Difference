class Solution {
    public char findTheDifference(String s, String t) {
        int sum=0;
        for(char i : t.toCharArray()) sum+=i;
        for(char i : s.toCharArray()) sum-=i;
        return (char) sum;
    }
}
