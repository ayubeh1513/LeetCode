class Solution {
    public int maxSubArray(int[] nums) {
        int cm = nums[0];
        int mm = nums[0];

        for(int i=1; i<nums.length; i++){
            cm = Math.max(nums[i], nums[i]+cm);
            mm = Math.max(cm, mm);
        }

        return mm;
    }
}
