class Solution {
    public int distributeCandies(int[] c) {
        Set<Integer> a = new HashSet<>();
        for(int i : c){
            a.add(i);
        }

        int u = a.size();
        return Math.min(u, c.length/2);
    }
}
